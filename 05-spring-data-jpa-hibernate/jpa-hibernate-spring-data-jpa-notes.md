# Difference Between JPA, Hibernate and Spring Data JPA

JPA is the specification that defines persistence annotations and contracts. Hibernate is an ORM implementation of the JPA specification. Spring Data JPA is a Spring abstraction over JPA/Hibernate that removes repository boilerplate through `JpaRepository`, derived query methods, and transaction integration.
